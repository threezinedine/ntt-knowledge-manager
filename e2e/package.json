{
	"name": "knowledge-manager-e2e",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"test:dev": "playwright test --config=playwright.dev.config.ts",
		"test:prod": "playwright test --config=playwright.prod.config.ts",
		"install:browsers": "playwright install chromium"
	},
	"devDependencies": {
		"@playwright/test": "^1.57.0"
	}
}
